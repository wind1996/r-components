import React from 'react';

export default ({ componentName }) => (
  <div style={{ marginTop: '10px' }}>
    <h3>使用方式</h3>
    <p>方式一：通过npm install （暂未实现这个）</p>
    <p>方式二：直接复制下面的JS文件和样式文件</p>
  </div>
);
